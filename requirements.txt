# Primary fetch path: plain HTTP + HTML parsing (no browser needed)
requests>=2.32.0
beautifulsoup4>=4.12.3

# Fallback only: used when the plain HTTP fetch fails or returns an
# unparseable page. Kept so a bot-protection change on the source site
# does not take the tracker down.
selenium>=4.25.0
